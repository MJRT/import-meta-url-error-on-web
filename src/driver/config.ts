import type { ElectricConfig } from "electric-sql/config";

// https://electric-sql.com/docs/api/clients/typescript#configuration
export const config: ElectricConfig = {
  url: "localhost",
  debug: true,
};
